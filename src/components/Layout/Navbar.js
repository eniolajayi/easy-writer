import React from "react";
import { Navbar } from "react-bootstrap";

const AppNavBar = () => {
  return (
    <Navbar bg="light" variant="light">
      <Navbar.Brand href="#">EasyDocs.</Navbar.Brand>
    </Navbar>
  );
};

export default AppNavBar;
